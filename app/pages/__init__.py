"""Multipage directory intentionally left empty.

The main app uses a custom navigation sidebar (``app.ui.sidebar``) and we do
not register any Streamlit multipage entries to avoid duplicate navigation
items in the mobile menu.
"""
from __future__ import annotations
